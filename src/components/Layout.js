import { Footer } from "Footer";
import { Header } from "Header";
import { Main } from "Main";

export function Layout() {

    return (
        <div>
            <Header />
            <Main />
            <Footer />
        </div>
    );
}